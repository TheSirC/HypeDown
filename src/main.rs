extern crate json;

fn affichage() {
    // Affiche l'aide si les options sont --help ou si elles ne contiennent pas --account ou --page et g�re les exceptions, renvoie les arguments passées au programme
    unimplemented!()
}

fn main() {
    (compte, nbpages,limite) = affichage();
    worker
}
